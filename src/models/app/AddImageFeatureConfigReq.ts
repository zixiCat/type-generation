export type AddImageFeatureConfigReq = {
  // 对应type的数据格式
  dataFormat?: string;
  // 特征ID
  featureName?: string;
  // 图片ID
  imageVersion?: string;
  // 备注
  remark?: string;
  // 类型
  type?: string;
};
